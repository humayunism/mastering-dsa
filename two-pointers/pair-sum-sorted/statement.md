# Pair with Target Sum

Given an **array of sorted numbers** and a **target sum**, find all pairs in the array whose sum equals the target.  

Return the pairs in **ascending order**.

---

## Example 1

```text
Input: [1, 2, 3, 4, 6], target = 6
Output: [[1, 5], [2, 4]]
